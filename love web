<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Will You Be Mine?</title>
  <style>
    :root{
      --bg1:#ffeef8; --bg2:#fff0f6; --accent:#ff6b9a; --accent2:#ff9ac2; --card:#ffffffcc;
      --shadow: 0 8px 30px rgba(14,10,20,0.08);
      --glass: rgba(255,255,255,0.7);
    }
    *{box-sizing:border-box}
    html,body{height:100%;margin:0;font-family:Inter, ui-sans-serif, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;color:#402233}
    body{
      background: radial-gradient(circle at 10% 10%, var(--bg1), var(--bg2) 40%);
      display:flex;align-items:center;justify-content:center;padding:28px;
      overflow:hidden;
    }
    .card{position:relative;z-index:2;max-width:720px;width:100%;background:linear-gradient(180deg, rgba(255,255,255,0.9), rgba(255,255,255,0.85));border-radius:20px;padding:28px;box-shadow:var(--shadow);backdrop-filter:blur(6px);
      display:grid;grid-template-columns:1fr 340px;gap:20px;align-items:center}
    .left{padding:12px}
    h1{margin:0;font-size:28px;letter-spacing:-0.5px}
    p.lead{margin:8px 0 18px;font-size:15px;color:#6b495a}
    .message-box{background:var(--card);padding:14px;border-radius:12px;box-shadow: 0 6px 18px rgba(250,100,160,0.06)}
    .emoji-row{display:flex;gap:8px;font-size:22px;margin-top:8px}
    .right{display:flex;flex-direction:column;align-items:center;justify-content:center;padding:18px}
    .proposal{text-align:center}
    .choices{display:flex;gap:8px;flex-wrap:wrap;justify-content:center;margin-top:16px;position:relative}
    .btn{border:0;padding:14px 22px;border-radius:14px;font-weight:700;font-size:16px;cursor:pointer;min-width:100px;box-shadow:0 6px 18px rgba(0,0,0,0.06);transition:transform .18s ease;display:inline-flex;gap:8px;align-items:center;justify-content:center;position:relative}
    .btn-yes{background:linear-gradient(135deg,var(--accent),var(--accent2));color:white}
    .btn-no{background:#fff;border:2px solid #ffd6e8;color:#ff4b7a;position:relative}
    .btn-angry{animation:shake 0.4s ease-in-out 0s 3 alternate}
    @keyframes shake{
      0%{transform:translateX(0)}
      25%{transform:translateX(-8px)}
      50%{transform:translateX(8px)}
      75%{transform:translateX(-8px)}
      100%{transform:translateX(0)}
    }
    .arrow-to-yes{position:absolute;top:-40px;left:50%;transform:translateX(-50%);font-size:24px;animation:bounce 1s infinite}
    @keyframes bounce{
      0%,100%{transform:translateX(-50%) translateY(0)}
      50%{transform:translateX(-50%) translateY(-8px)}
    }
    .result{margin-top:16px;font-size:18px;font-weight:700}
    #confetti{position:fixed;left:0;top:0;width:100%;height:100%;pointer-events:none;z-index:3}
    .celebration{position:fixed;inset:0;display:flex;align-items:center;justify-content:center;background:rgba(255,255,255,0.7);font-size:40px;font-weight:bold;color:#ff4b7a;z-index:4;animation:fadeOut 2.5s forwards}
    @keyframes fadeOut{0%{opacity:1}100%{opacity:0;visibility:hidden}}
    @media (max-width:780px){.card{grid-template-columns:1fr;}h1{font-size:22px}}
  </style>
</head>
<body>
  <div class="card">
    <div class="left">
      <h1>Will you be my sweetie? 💖</h1>
      <p class="lead">This little page is just for you — a tiny heart-filled question. Choose your answer and watch the magic happen ✨</p>
      <div class="message-box"><div style="font-size:14px">From:</div><div style="font-weight:700;margin-top:6px">Someone who adores you 💌</div><div class="emoji-row"><span>😍</span><span>🥰</span><span>😘</span><span>🌸</span><span>🍫</span></div></div>
    </div>
    <div class="right">
      <div class="proposal">
        <img src="data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' width='100' height='100' viewBox='0 0 24 24'><path fill='%23ff6b9a' d='M12 21s-7-4.35-9-7.5C1.5 9.75 4 6 7.5 6 9.6 6 12 8 12 8s2.4-2 4.5-2C20 6 22.5 9.75 21 13.5 19 16.65 12 21 12 21z'/></svg>" style="width:100px;height:100px;margin-bottom:12px"/>
        <div style="font-size:15px;color:#6b495a">Do you say <strong>Yes</strong> or <strong>No</strong>?</div>
        <div class="choices">
          <div id="arrow" class="arrow-to-yes" style="display:none">⬇️</div>
          <button class="btn btn-yes" id="yesBtn">Yes ❤️‍🔥</button>
          <button class="btn btn-no" id="noBtn">No 😿</button>
        </div>
        <div id="result" class="result"></div>
      </div>
    </div>
  </div>
  <canvas id="confetti"></canvas>
  <script>
    const noBtn = document.getElementById('noBtn');
    const yesBtn = document.getElementById('yesBtn');
    const result = document.getElementById('result');
    const arrow = document.getElementById('arrow');
    let noClicks = 0;
    let continuousMoveInterval = null;

    yesBtn.addEventListener('click', ()=>{
      result.textContent = 'YAY! 💕 I choose you too!';
      yesBtn.disabled = true; noBtn.disabled = true;
      arrow.style.display = 'none';
      sprinkleConfetti();
      showCelebration();
    });

    noBtn.addEventListener('click', ()=>{
      noClicks++;
      alert('😭 Please say Yes!');
      if(noClicks>=6 && noClicks<=8){
        startContinuousMovement();
        arrow.style.display = 'block';
      } else {
        moveNoButtonNearYes();
      }
      if(noClicks === 5){
        triggerAngryAnimation();
      }
      result.textContent = "Aww... 😿 That's okay. I still think you're amazing.";
    });

    function moveNoButtonNearYes(){
      const yesRect = yesBtn.getBoundingClientRect();
      const btn = noBtn;
      const offsetX = (Math.random() * 120) - 60; 
      const offsetY = (Math.random() * 80) - 40;
      btn.style.position = 'fixed';
      btn.style.left = yesRect.left + offsetX + 'px';
      btn.style.top = yesRect.top + offsetY + 'px';
    }

    function startContinuousMovement(){
      if(continuousMoveInterval) return;
      continuousMoveInterval = setInterval(()=>{
        moveNoButtonNearYes();
      },500);
    }

    function triggerAngryAnimation(){
      noBtn.classList.add('btn-angry');
      setTimeout(()=> noBtn.classList.remove('btn-angry'), 1200);
    }

    function showCelebration(){
      const celebration = document.createElement('div');
      celebration.className = 'celebration';
      celebration.textContent = '🎉 You made me so happy! 🎉';
      document.body.appendChild(celebration);
      setTimeout(()=>celebration.remove(),2500);
    }

    function sprinkleConfetti(){
      const canvas = document.getElementById('confetti');
      const ctx = canvas.getContext('2d');
      let W = canvas.width = innerWidth;
      let H = canvas.height = innerHeight;
      window.addEventListener('resize', ()=>{W=canvas.width=innerWidth;H=canvas.height=innerHeight});
      const pieces = [];
      const colors = ['#ff6b9a','#ffd1e8','#ffcf7b','#ff9ac2','#ffd6e8'];
      for(let i=0;i<200;i++){
        pieces.push({x:W/2,y:H/2,vx:Math.random()*14-7,vy:Math.random()*-14,size:Math.random()*8+6,color:colors[Math.floor(Math.random()*colors.length)],rot:Math.random()*360,vr:Math.random()*10-5});
      }
      let t=0;
      function frame(){
        ctx.clearRect(0,0,W,H);
        for(let p of pieces){
          p.x += p.vx; p.y += p.vy; p.vy += 0.25; p.rot += p.vr;
          ctx.save(); ctx.translate(p.x,p.y); ctx.rotate(p.rot*Math.PI/180);
          ctx.fillStyle = p.color; ctx.fillRect(-p.size/2,-p.size/2,p.size,p.size*0.6);
          ctx.restore();
        }
        t++;
        if(t<220) requestAnimationFrame(frame);
      }
      frame();
    }
  </script>
</body>
</html>
